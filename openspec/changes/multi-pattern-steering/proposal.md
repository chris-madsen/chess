# Multi-target pattern steering

Add bounded parallel PatternSteeredTalPath lines with a fixed target PatternFamily.
The existing single adaptive line remains available, while raw-game and dataset
CLI runs may request automatic or explicit target families.

